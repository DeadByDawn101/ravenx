# RavenOS API + UI configuration

# Where OpenClaw gateway is reachable from the API container.
RAVENOS_GATEWAY_URL=http://localhost:18789

# Gateway token/password. Keep in .env, do not hardcode in UI.
# If you prefer to paste token into UI, leave this blank and run API in "pass-through" mode.
RAVENOS_GATEWAY_TOKEN=

# RavenOS runtime mode
# studio = persistent volumes; baas = ephemeral-ish defaults (you can add tmpfs in compose)
RAVENOS_MODE=studio

# API server
PORT=8787

# Optional: direct-install into OpenClaw directories (mount these into the api container)
RAVENOS_OPENCLAW_AGENTS_DIR=
RAVENOS_OPENCLAW_SKILLS_DIR=
RAVENOS_OPENCLAW_PLUGINS_DIR=
RAVENOS_PACKS_DIR=/var/lib/ravenos/packs
RAVENOS_REPORTS_DIR=/var/lib/ravenos/reports
# Lease extension token HMAC secret (required for BaaS timelock extensions)
# Generate: openssl rand -hex 32
RAVENX_EXTENSION_SECRET=
